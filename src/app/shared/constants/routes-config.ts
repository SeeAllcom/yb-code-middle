export class RoutesConfig {
  static root = '';
  static products = 'products';
  static productsList = `${RoutesConfig.products}/list`;
  static productsSelected = `${RoutesConfig.products}/selected`;
}
